{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksVersion": "1.41.0",
    "configFile": "production",
    "nixPkgs": ["nodejs_20", "openssl"],
    "buildCommand": "corepack enable && corepack prepare pnpm@9 --activate && pnpm install --frozen-lockfile && pnpm --filter database db:generate && pnpm --filter server build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "corepack enable && corepack prepare pnpm@9 --activate && pnpm --filter database db:deploy && pnpm --filter server start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}