databases:
  - name: wto3-db
    plan: free

services:
  # السيرفر (Socket.io Backend)
  - type: web
    name: wto3-server
    env: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install && pnpm --filter database db:generate && pnpm --filter database db:push && pnpm --filter server build
    startCommand: pnpm --filter server start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: PNPM_VERSION
        value: "9"
      - key: DATABASE_URL
        fromDatabase:
          name: wto3-db
          property: connectionString
      - key: ALLOWED_ORIGINS
        fromService:
          type: web
          name: wto3
          envVarKey: RENDER_EXTERNAL_URL
      # المتغيرات السرية للسيرفر (سيطلبها منك ريندر)
      - key: JWT_SECRET
        sync: false
      - key: ENCRYPTION_KEY
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false

  # الواجهة (Next.js Frontend)
  - type: web
    name: wto3
    env: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install && pnpm --filter database db:generate && pnpm --filter web build
    startCommand: pnpm --filter web start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: PNPM_VERSION
        value: "9"
      - key: NEXT_PUBLIC_SERVER_URL
        fromService:
          type: web
          name: wto3-server
          envVarKey: RENDER_EXTERNAL_URL
      - key: DATABASE_URL
        fromDatabase:
          name: wto3-db
          property: connectionString
      - key: NEXTAUTH_URL
        fromService:
          type: web
          name: wto3
          envVarKey: RENDER_EXTERNAL_URL
      # المتغيرات السرية للواجهة (سيطلبها منك ريندر)
      - key: NEXT_PUBLIC_YOUTUBE_API_KEY
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: NEXTAUTH_SECRET
        sync: false
